package lu.magalhaes.gilles.provxlib
package benchmark.utils

object NtfyNotifier {

  def notify(
      title: String,
      message: String = "",
      emoji: Option[String] = None
  ): Unit = {
//    val emojiHeader = if (emoji.isDefined) {
//      Map("Tag" -> emoji.get)
//    } else {
//      Map()
//    }
//    requests.post(
//      "https://ntfy.magalhaes.lu/thesis",
//      headers = Map(
//        "Title" -> title
//      ) ++ emojiHeader,
//      data = message
//    )
  }

}
